<div class="author">
    <h1 class="author_heading">Welcome to Next Tech Site</h1>
    <p class="author_body">Let us come together and celebrate our shared passion for technology through this exciting endeavor.
                           I am confident that this platform will foster a sense of community and empower individuals to connect,
                           learn, and grow within the tech industry.</p>
</div>

<img src="/img/question.png" alt="question yourself" class="question-image">

<section class="articles">
    <h2 class="articles_heading">Latest Posts</h2>

    <ul class="article-ul">
        <% data.forEach(post => { %>
        <li>
            <a href="/post/<%= post._id %>">
                <span><%= post.title %></span>
                <span class="article-list_date"><%= post.createdAt.toDateString() %></span>
            </a>
        </li>
        <% }) %>
    </ul>

    <% if (nextPage !== null) { %>
        <a href="/?page=<%= nextPage %>" class="pagination">&lt; View Older Posts</a>
    <% } %>
    
</section>